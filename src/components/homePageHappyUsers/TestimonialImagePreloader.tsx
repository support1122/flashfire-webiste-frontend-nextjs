// Testimonial images are below-fold — browser lazy loading handles them.
// This component is kept as a no-op to avoid breaking imports.
export default function TestimonialImagePreloader() {
  return null;
}
